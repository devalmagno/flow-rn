import { View } from 'react-native';
import EpisodeType from '../types/EpisodeType';

interface Props {
    episode: EpisodeType;
}

export default function PausedDownload({ episode }: Props) {
    return (
        <View>

        </View>
    );
}